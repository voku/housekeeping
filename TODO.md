# TODO

- Add opt-in smoke tests that exercise real Codex, Gemini, and Copilot CLIs when those binaries are available in CI or on a developer workstation.
- Normalize provider output further so tasks can persist structured summaries or patch metadata instead of only raw stdout/stderr.
- Add task-level provider routing rules for cases where one provider should be preferred for a specific maintenance task regardless of the global readiness ranking.
